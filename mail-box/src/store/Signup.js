import { createSlice } from "@reduxjs/toolkit";

const SignUpState = {
email:"",
password:""
}

const SignUpSlice = createSlice({
    name:"SignUp",
    initialState: SignUpState,
    reducers:{
     
    }
})